# Nest PI backend uses global prefix "api" (see main.ts). Use host only if your API has no prefix (e.g. some arena_chain setups).
VITE_API_URL=http://localhost:3000/api




VITE_API_URL=http://localhost:3000

