/** Shared slow “cinema” timing for login (LoL) + register (Valorant) backdrops */
export const AUTH_SLIDE_INTERVAL_MS = 22_000;
/** Crossfade between slides (seconds) */
export const AUTH_CROSSFADE_SECONDS = 7.5;
/** Ken Burns zoom per slide (seconds, linear) */
export const AUTH_ZOOM_SECONDS = 26;

/** Register (Valorant): quicker slideshow — login stays on AUTH_* above */
export const REGISTER_SLIDE_INTERVAL_MS = 2000;
export const REGISTER_CROSSFADE_SECONDS = 1.15;
export const REGISTER_ZOOM_SECONDS = 9;
